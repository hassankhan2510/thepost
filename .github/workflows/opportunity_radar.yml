name: 🎯 Opportunity Radar

on:
  # DISABLED 2026-08-23 — replaced by personal_radar.yml (Personal Radar v2).
  # Daily schedule turned off so it no longer posts to Discord. Kept for manual runs only.
  # schedule:
  #   - cron: '0 3 * * *'   # Daily at 8:00 AM PKT (3:00 AM UTC)
  workflow_dispatch: {}    # Manual trigger only (no automatic runs)

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔍 Run Opportunity Radar
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          JINA_API_KEY: ${{ secrets.JINA_API_KEY }}
          DISCORD_RADAR_WEBHOOK: ${{ secrets.DISCORD_RADAR_WEBHOOK }}
        run: node scripts/opportunity_radar.mjs

      - name: 📁 Upload report as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: opportunity-report-${{ github.run_id }}
          path: |
            out/opportunity_report.md
            out/opportunities_raw.json
          if-no-files-found: ignore
          retention-days: 30
